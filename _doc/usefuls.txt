git ini
git status
git add .  "add all modified & untracked"
git commit -a -m "msg"  "-a is add modified only, -am short-hand"
git commit --amend  "amend commit comment"
git log --oneline
git config -l --global/system/local
git branch -d branch-name "-d is delete; -m is rename"
git switch -c branch-name  "-c is create"
git switch master, git merge branch-name
git diff
git diff commithash1..commithash2
git stash
git stash pop
git stash apply
git stash list
git checkout HEAD~1 || git switch -   "go back one commit"
git checkout HEAD <filename>  "discard changes to last commit"